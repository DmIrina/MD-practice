apiVersion: v1
kind: PersistentVolumeClaim	# Create a PersistentVolumeClaim to request a Persis
metadata:		# Claim name and labels
  name: pv-claim
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi